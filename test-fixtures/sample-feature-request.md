# Sample Feature Request

Add validation to a user profile form so empty display names cannot be saved.

Expected behavior:

- Empty or whitespace-only names are rejected.
- A clear validation message is shown.
- Existing valid saves keep working.
